dataset_analyst_task:
  agent: dataset_analyst
  description: |
    Проведи анализ датасета.

    Используй следующие параметры:

    ID датасета: {dataset_id}
    ID версии датасета: {dataset_version_id}

    ТВОИ ЗАДАЧИ:
    - Оценить качество датасета
    - Найти возможные проблемы
    - Оценить готовность к обучению
    - Указать ограничения и риски
    - Дать рекомендации

    ВАЖНО:
    - Если не удалось найти указанную версию это значит, что датасет не готов к обучению
    - Используй доступные инструменты
    - Не придумывай данные
    - Работай ТОЛЬКО с указанным датасетом
    - Если информации недостаточно - сообщи об этом

  expected_output: |
    Твой ответ должен быть строго в формате JSON, соответствующем схеме DatasetAnalystOut.
    
    ВАЖНО:
    - readiness_assessment: true если датасет готов к обучению, false если не готов
    - Проблемы и рекомендации перечисляй через \n для каждой новой строки
      